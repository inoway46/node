# Fork-only experiment; not part of the upstream runner-change PR.
name: Benchmark tarball gzip-6 - 4 batches of 64

on:
  push:
    branches: [codex/benchmark-gzip6-256]

permissions:
  contents: read

env:
  PYTHON_VERSION: '3.14'
  NODE_VERSION: lts/*
  DISTTYPE: nightly
  DATESTRING: '2026-09-23'
  SOURCE_SHA: 2c2aafe04d80935fd7d9c6e73fb7fdcf52b4d268
  LEVEL: 6

jobs:
  batch-1:
    name: gzip-6 / batch-1 / sample-${{ matrix.n }}
    runs-on: ubuntu-slim
    strategy:
      fail-fast: false
      max-parallel: 12
      matrix:
        n: [
          0x00,
          0x01,
          0x02,
          0x03,
          0x04,
          0x05,
          0x06,
          0x07,
          0x08,
          0x09,
          0x0a,
          0x0b,
          0x0c,
          0x0d,
          0x0e,
          0x0f,
          0x10,
          0x11,
          0x12,
          0x13,
          0x14,
          0x15,
          0x16,
          0x17,
          0x18,
          0x19,
          0x1a,
          0x1b,
          0x1c,
          0x1d,
          0x1e,
          0x1f,
          0x20,
          0x21,
          0x22,
          0x23,
          0x24,
          0x25,
          0x26,
          0x27,
          0x28,
          0x29,
          0x2a,
          0x2b,
          0x2c,
          0x2d,
          0x2e,
          0x2f,
          0x30,
          0x31,
          0x32,
          0x33,
          0x34,
          0x35,
          0x36,
          0x37,
          0x38,
          0x39,
          0x3a,
          0x3b,
          0x3c,
          0x3d,
          0x3e,
          0x3f,
        ]
    env:
      BATCH: 1
      SAMPLE: ${{ matrix.n }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
          ref: ${{ env.SOURCE_SHA }}
      - name: Set up Python ${{ env.PYTHON_VERSION }}
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          allow-prereleases: true
      - name: Use Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: ${{ env.NODE_VERSION }}
      - name: Make tarball
        run: |
          echo "BENCHMARK_LEVEL=$LEVEL BENCHMARK_BATCH=$BATCH BENCHMARK_SAMPLE=$SAMPLE SOURCE_SHA=$SOURCE_SHA"
          /usr/bin/gzip --version
          export COMMIT=$(git rev-parse --short=10 "$SOURCE_SHA")
          mkdir -p "$RUNNER_TEMP/timed-bin"
          cat > "$RUNNER_TEMP/timed-bin/gzip" <<'SCRIPT'
          #!/bin/sh
          /usr/bin/time -f 'GZIP_SECONDS=%e GZIP_USER_SECONDS=%U GZIP_SYSTEM_SECONDS=%S' -o "$RUNNER_TEMP/gzip.time" /usr/bin/gzip "$@"
          status=$?
          cat "$RUNNER_TEMP/gzip.time" >&2
          exit "$status"
          SCRIPT
          chmod +x "$RUNNER_TEMP/timed-bin/gzip"
          export PATH="$RUNNER_TEMP/timed-bin:$PATH"
          /usr/bin/time -f 'CONFIGURE_SECONDS=%e' -o "$RUNNER_TEMP/configure.time" ./configure
          /usr/bin/time -f 'MAKE_TAR_SECONDS=%e' -o "$RUNNER_TEMP/make.time" make tar -j4 SKIP_XZ=1 GZIP_COMPRESSION="$LEVEL"
          stat --format='TARBALL_BYTES=%s' ./*.tar.gz | tee "$RUNNER_TEMP/size.txt"
          # archive:false uses the filename as the actual artifact name.
          mkdir -p benchmark-artifact
          printf -v artifact_name 'gzip-%s-batch-%s-sample-%02d.tar.gz' "$LEVEL" "$BATCH" "$SAMPLE"
          mv ./*.tar.gz "benchmark-artifact/$artifact_name"
      - name: Upload tarball artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          path: benchmark-artifact/*.tar.gz
          archive: false
          retention-days: 1
          if-no-files-found: error
      - name: Record available measurements
        if: always()
        run: |
          {
            echo "Level: $LEVEL; batch: $BATCH; sample: $SAMPLE; source: $SOURCE_SHA"
            echo '```text'
            for metric in configure.time gzip.time make.time size.txt; do
              if [ -f "$RUNNER_TEMP/$metric" ]; then
                cat "$RUNNER_TEMP/$metric"
              fi
            done
            echo '```'
          } | tee -a "$GITHUB_STEP_SUMMARY"

  batch-2:
    name: gzip-6 / batch-2 / sample-${{ matrix.n }}
    needs: batch-1
    if: ${{ !cancelled() }}
    runs-on: ubuntu-slim
    strategy:
      fail-fast: false
      max-parallel: 12
      matrix:
        n: [
          0x00,
          0x01,
          0x02,
          0x03,
          0x04,
          0x05,
          0x06,
          0x07,
          0x08,
          0x09,
          0x0a,
          0x0b,
          0x0c,
          0x0d,
          0x0e,
          0x0f,
          0x10,
          0x11,
          0x12,
          0x13,
          0x14,
          0x15,
          0x16,
          0x17,
          0x18,
          0x19,
          0x1a,
          0x1b,
          0x1c,
          0x1d,
          0x1e,
          0x1f,
          0x20,
          0x21,
          0x22,
          0x23,
          0x24,
          0x25,
          0x26,
          0x27,
          0x28,
          0x29,
          0x2a,
          0x2b,
          0x2c,
          0x2d,
          0x2e,
          0x2f,
          0x30,
          0x31,
          0x32,
          0x33,
          0x34,
          0x35,
          0x36,
          0x37,
          0x38,
          0x39,
          0x3a,
          0x3b,
          0x3c,
          0x3d,
          0x3e,
          0x3f,
        ]
    env:
      BATCH: 2
      SAMPLE: ${{ matrix.n }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
          ref: ${{ env.SOURCE_SHA }}
      - name: Set up Python ${{ env.PYTHON_VERSION }}
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          allow-prereleases: true
      - name: Use Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: ${{ env.NODE_VERSION }}
      - name: Make tarball
        run: |
          echo "BENCHMARK_LEVEL=$LEVEL BENCHMARK_BATCH=$BATCH BENCHMARK_SAMPLE=$SAMPLE SOURCE_SHA=$SOURCE_SHA"
          /usr/bin/gzip --version
          export COMMIT=$(git rev-parse --short=10 "$SOURCE_SHA")
          mkdir -p "$RUNNER_TEMP/timed-bin"
          cat > "$RUNNER_TEMP/timed-bin/gzip" <<'SCRIPT'
          #!/bin/sh
          /usr/bin/time -f 'GZIP_SECONDS=%e GZIP_USER_SECONDS=%U GZIP_SYSTEM_SECONDS=%S' -o "$RUNNER_TEMP/gzip.time" /usr/bin/gzip "$@"
          status=$?
          cat "$RUNNER_TEMP/gzip.time" >&2
          exit "$status"
          SCRIPT
          chmod +x "$RUNNER_TEMP/timed-bin/gzip"
          export PATH="$RUNNER_TEMP/timed-bin:$PATH"
          /usr/bin/time -f 'CONFIGURE_SECONDS=%e' -o "$RUNNER_TEMP/configure.time" ./configure
          /usr/bin/time -f 'MAKE_TAR_SECONDS=%e' -o "$RUNNER_TEMP/make.time" make tar -j4 SKIP_XZ=1 GZIP_COMPRESSION="$LEVEL"
          stat --format='TARBALL_BYTES=%s' ./*.tar.gz | tee "$RUNNER_TEMP/size.txt"
          # archive:false uses the filename as the actual artifact name.
          mkdir -p benchmark-artifact
          printf -v artifact_name 'gzip-%s-batch-%s-sample-%02d.tar.gz' "$LEVEL" "$BATCH" "$SAMPLE"
          mv ./*.tar.gz "benchmark-artifact/$artifact_name"
      - name: Upload tarball artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          path: benchmark-artifact/*.tar.gz
          archive: false
          retention-days: 1
          if-no-files-found: error
      - name: Record available measurements
        if: always()
        run: |
          {
            echo "Level: $LEVEL; batch: $BATCH; sample: $SAMPLE; source: $SOURCE_SHA"
            echo '```text'
            for metric in configure.time gzip.time make.time size.txt; do
              if [ -f "$RUNNER_TEMP/$metric" ]; then
                cat "$RUNNER_TEMP/$metric"
              fi
            done
            echo '```'
          } | tee -a "$GITHUB_STEP_SUMMARY"

  batch-3:
    name: gzip-6 / batch-3 / sample-${{ matrix.n }}
    needs: batch-2
    if: ${{ !cancelled() }}
    runs-on: ubuntu-slim
    strategy:
      fail-fast: false
      max-parallel: 12
      matrix:
        n: [
          0x00,
          0x01,
          0x02,
          0x03,
          0x04,
          0x05,
          0x06,
          0x07,
          0x08,
          0x09,
          0x0a,
          0x0b,
          0x0c,
          0x0d,
          0x0e,
          0x0f,
          0x10,
          0x11,
          0x12,
          0x13,
          0x14,
          0x15,
          0x16,
          0x17,
          0x18,
          0x19,
          0x1a,
          0x1b,
          0x1c,
          0x1d,
          0x1e,
          0x1f,
          0x20,
          0x21,
          0x22,
          0x23,
          0x24,
          0x25,
          0x26,
          0x27,
          0x28,
          0x29,
          0x2a,
          0x2b,
          0x2c,
          0x2d,
          0x2e,
          0x2f,
          0x30,
          0x31,
          0x32,
          0x33,
          0x34,
          0x35,
          0x36,
          0x37,
          0x38,
          0x39,
          0x3a,
          0x3b,
          0x3c,
          0x3d,
          0x3e,
          0x3f,
        ]
    env:
      BATCH: 3
      SAMPLE: ${{ matrix.n }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
          ref: ${{ env.SOURCE_SHA }}
      - name: Set up Python ${{ env.PYTHON_VERSION }}
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          allow-prereleases: true
      - name: Use Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: ${{ env.NODE_VERSION }}
      - name: Make tarball
        run: |
          echo "BENCHMARK_LEVEL=$LEVEL BENCHMARK_BATCH=$BATCH BENCHMARK_SAMPLE=$SAMPLE SOURCE_SHA=$SOURCE_SHA"
          /usr/bin/gzip --version
          export COMMIT=$(git rev-parse --short=10 "$SOURCE_SHA")
          mkdir -p "$RUNNER_TEMP/timed-bin"
          cat > "$RUNNER_TEMP/timed-bin/gzip" <<'SCRIPT'
          #!/bin/sh
          /usr/bin/time -f 'GZIP_SECONDS=%e GZIP_USER_SECONDS=%U GZIP_SYSTEM_SECONDS=%S' -o "$RUNNER_TEMP/gzip.time" /usr/bin/gzip "$@"
          status=$?
          cat "$RUNNER_TEMP/gzip.time" >&2
          exit "$status"
          SCRIPT
          chmod +x "$RUNNER_TEMP/timed-bin/gzip"
          export PATH="$RUNNER_TEMP/timed-bin:$PATH"
          /usr/bin/time -f 'CONFIGURE_SECONDS=%e' -o "$RUNNER_TEMP/configure.time" ./configure
          /usr/bin/time -f 'MAKE_TAR_SECONDS=%e' -o "$RUNNER_TEMP/make.time" make tar -j4 SKIP_XZ=1 GZIP_COMPRESSION="$LEVEL"
          stat --format='TARBALL_BYTES=%s' ./*.tar.gz | tee "$RUNNER_TEMP/size.txt"
          # archive:false uses the filename as the actual artifact name.
          mkdir -p benchmark-artifact
          printf -v artifact_name 'gzip-%s-batch-%s-sample-%02d.tar.gz' "$LEVEL" "$BATCH" "$SAMPLE"
          mv ./*.tar.gz "benchmark-artifact/$artifact_name"
      - name: Upload tarball artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          path: benchmark-artifact/*.tar.gz
          archive: false
          retention-days: 1
          if-no-files-found: error
      - name: Record available measurements
        if: always()
        run: |
          {
            echo "Level: $LEVEL; batch: $BATCH; sample: $SAMPLE; source: $SOURCE_SHA"
            echo '```text'
            for metric in configure.time gzip.time make.time size.txt; do
              if [ -f "$RUNNER_TEMP/$metric" ]; then
                cat "$RUNNER_TEMP/$metric"
              fi
            done
            echo '```'
          } | tee -a "$GITHUB_STEP_SUMMARY"

  batch-4:
    name: gzip-6 / batch-4 / sample-${{ matrix.n }}
    needs: batch-3
    if: ${{ !cancelled() }}
    runs-on: ubuntu-slim
    strategy:
      fail-fast: false
      max-parallel: 12
      matrix:
        n: [
          0x00,
          0x01,
          0x02,
          0x03,
          0x04,
          0x05,
          0x06,
          0x07,
          0x08,
          0x09,
          0x0a,
          0x0b,
          0x0c,
          0x0d,
          0x0e,
          0x0f,
          0x10,
          0x11,
          0x12,
          0x13,
          0x14,
          0x15,
          0x16,
          0x17,
          0x18,
          0x19,
          0x1a,
          0x1b,
          0x1c,
          0x1d,
          0x1e,
          0x1f,
          0x20,
          0x21,
          0x22,
          0x23,
          0x24,
          0x25,
          0x26,
          0x27,
          0x28,
          0x29,
          0x2a,
          0x2b,
          0x2c,
          0x2d,
          0x2e,
          0x2f,
          0x30,
          0x31,
          0x32,
          0x33,
          0x34,
          0x35,
          0x36,
          0x37,
          0x38,
          0x39,
          0x3a,
          0x3b,
          0x3c,
          0x3d,
          0x3e,
          0x3f,
        ]
    env:
      BATCH: 4
      SAMPLE: ${{ matrix.n }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false
          ref: ${{ env.SOURCE_SHA }}
      - name: Set up Python ${{ env.PYTHON_VERSION }}
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: ${{ env.PYTHON_VERSION }}
          allow-prereleases: true
      - name: Use Node.js ${{ env.NODE_VERSION }}
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: ${{ env.NODE_VERSION }}
      - name: Make tarball
        run: |
          echo "BENCHMARK_LEVEL=$LEVEL BENCHMARK_BATCH=$BATCH BENCHMARK_SAMPLE=$SAMPLE SOURCE_SHA=$SOURCE_SHA"
          /usr/bin/gzip --version
          export COMMIT=$(git rev-parse --short=10 "$SOURCE_SHA")
          mkdir -p "$RUNNER_TEMP/timed-bin"
          cat > "$RUNNER_TEMP/timed-bin/gzip" <<'SCRIPT'
          #!/bin/sh
          /usr/bin/time -f 'GZIP_SECONDS=%e GZIP_USER_SECONDS=%U GZIP_SYSTEM_SECONDS=%S' -o "$RUNNER_TEMP/gzip.time" /usr/bin/gzip "$@"
          status=$?
          cat "$RUNNER_TEMP/gzip.time" >&2
          exit "$status"
          SCRIPT
          chmod +x "$RUNNER_TEMP/timed-bin/gzip"
          export PATH="$RUNNER_TEMP/timed-bin:$PATH"
          /usr/bin/time -f 'CONFIGURE_SECONDS=%e' -o "$RUNNER_TEMP/configure.time" ./configure
          /usr/bin/time -f 'MAKE_TAR_SECONDS=%e' -o "$RUNNER_TEMP/make.time" make tar -j4 SKIP_XZ=1 GZIP_COMPRESSION="$LEVEL"
          stat --format='TARBALL_BYTES=%s' ./*.tar.gz | tee "$RUNNER_TEMP/size.txt"
          # archive:false uses the filename as the actual artifact name.
          mkdir -p benchmark-artifact
          printf -v artifact_name 'gzip-%s-batch-%s-sample-%02d.tar.gz' "$LEVEL" "$BATCH" "$SAMPLE"
          mv ./*.tar.gz "benchmark-artifact/$artifact_name"
      - name: Upload tarball artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          path: benchmark-artifact/*.tar.gz
          archive: false
          retention-days: 1
          if-no-files-found: error
      - name: Record available measurements
        if: always()
        run: |
          {
            echo "Level: $LEVEL; batch: $BATCH; sample: $SAMPLE; source: $SOURCE_SHA"
            echo '```text'
            for metric in configure.time gzip.time make.time size.txt; do
              if [ -f "$RUNNER_TEMP/$metric" ]; then
                cat "$RUNNER_TEMP/$metric"
              fi
            done
            echo '```'
          } | tee -a "$GITHUB_STEP_SUMMARY"
